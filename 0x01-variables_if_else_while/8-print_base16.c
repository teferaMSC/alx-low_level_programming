#include<stdio.h>
/**
 * main -main
 * description : prints all singel number of base 10
 * starting from 0 to 9
 * Return : always
 */
int main(void)
{
		for (ch = '0'; ch <= '9'; c++)
		{
		putcha(ch);
		}
		for (ch = 'a'; ch <= 'f'; ch++)
		{
		puctchar(ch);
		}
		puchchar(ch);
		return (0);
		}
