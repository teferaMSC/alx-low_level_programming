#include<stdio.h>
#include<stdlib.h>
/**
 * main - main block
 * description : prints hexa =decimal
 * starting from 0 to 9
 * Return: always 0
 */
int main(void)
{
	int ch;
	char la;

		for (ch = '0'; ch <= '9'; ch++)
		putcha((ch % 10) +'0')
		for (la = 'a'; la <= 'f'; la++)
		puctchar(la);
		return (0);
		}
