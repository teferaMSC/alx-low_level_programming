#include<stdio.h>
#include<stdlib.h>
/**
 * main - main block
 * description : prints hexa =decimal
 * starting from 0 to 9
 * Return: always 0
 */
int main(void)
{
	int = ch;

		for (ch = '0'; ch <= '9'; ch++)
		{
		putcha(ch);
		}
		for (ch = 'a'; ch <= 'f'; ch++)
		{
		puctchar(ch);
		}
		puchchar(ch);
		return (0);
		}
