#include "main.h"
/**
 * _abs - absolut value of I
 * @c: number
 * Return: Absolute values
 */
int _abs(int c)
{
	if (x < 0)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
	return (c);
}

